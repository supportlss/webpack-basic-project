import "./test.scss";

function test() {
  var root = document.getElementsByClassName("root")[0];
  var span = document.createElement("span");
  var text = document.createTextNode("webpack-basic-project");
  p.appendChild(text);
  root.appendChild(span);
}

test();